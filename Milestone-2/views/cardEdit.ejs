<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Card</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="/js/script.js" defer></script>
    <style>
        body {
            background-image: url('/assets/card-edit-bg.jpg');
            background-size: cover;
            background-position: center center;
            margin: 0;
            padding: 0;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>
    <header class="card-edit-header">
        <h1>Edit Card Details</h1>
    </header>
    <main class="card-edit-main">
        <section class="card-edit-section">
            <form action="/cards/<%= card.id %>/edit" method="post" class="card-edit-form">
                <fieldset class="card-edit-fieldset">
                    <legend class="card-edit-legend">Card Information</legend>
                    <div class="form-group">
                        <label for="name" class="card-edit-label">Name:</label>
                        <input type="text" id="name" name="name" value="<%= card.name %>" required class="card-edit-input">
                    </div>

                    <div class="form-group">
                        <label for="description" class="card-edit-label">Description:</label>
                        <textarea id="description" name="description" required class="card-edit-textarea"><%= card.description %></textarea>
                    </div>

                    <div class="form-group">
                        <label for="attack_points" class="card-edit-label">Attack Points:</label>
                        <input type="number" id="attack_points" name="attack_points" value="<%= card.attack_points %>" required class="card-edit-input">
                    </div>

                    <div class="form-group">
                        <label for="defence_points" class="card-edit-label">Defence Points:</label>
                        <input type="number" id="defence_points" name="defence_points" value="<%= card.defence_points %>" required class="card-edit-input">
                    </div>

                    <div class="form-group">
                        <label for="type" class="card-edit-label">Type:</label>
                        <select id="type" name="type" required class="card-edit-select">
                            <option value="Normal" <%= card.type === 'Normal' ? 'selected' : '' %>>Normal</option>
                            <option value="Effect" <%= card.type === 'Effect' ? 'selected' : '' %>>Effect</option>
                            <option value="Ritual" <%= card.type === 'Ritual' ? 'selected' : '' %>>Ritual</option>
                            <option value="Synchro" <%= card.type === 'Synchro' ? 'selected' : '' %>>Synchro</option>
                            <option value="Fusion" <%= card.type === 'Fusion' ? 'selected' : '' %>>Fusion</option>
                            <option value="Dark-Synchro" <%= card.type === 'Dark-Synchro' ? 'selected' : '' %>>Dark Synchro</option>
                            <option value="XYZ" <%= card.type === 'XYZ' ? 'selected' : '' %>>XYZ</option>
                            <option value="Link" <%= card.type === 'Link' ? 'selected' : '' %>>Link</option>
                            <option value="Spell" <%= card.type === 'Spell' ? 'selected' : '' %>>Spell</option>
                            <option value="Trap" <%= card.type === 'Trap' ? 'selected' : '' %>>Trap</option>
                        </select>
                    </div>

                    <div class="form-group">
                        <label for="is_synchro" class="card-edit-label">Is Synchro:</label>
                        <input type="checkbox" id="is_synchro" name="is_synchro" class="card-edit-checkbox" <%= card.is_synchro ? 'checked' : '' %>>
                    </div>
                </fieldset>

                <footer class="card-edit-footer">
                    <button type="submit" class="card-edit-button">Save Changes</button>
                </footer>
            </form>
        </section>
    </main>
</body>
</html>
